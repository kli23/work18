#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#define READ 0
#define WRITE 1


int main() {
    int fds[2];
    int fds2[2];
    pipe( fds );
    pipe( fds2 );
    char line[100];

    f = fork();
    if (f == 0) {

    }
    else {
        
    }


}
